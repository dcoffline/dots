[Unit]
Description=Jottacloud Daemon inside Athena Distrobox
After=podman.service

[Service]
Type=simple
ExecStart=/usr/bin/distrobox-enter -n athena -- jottad datadir /var/home/eric/.local/share/jottad
Restart=always

[Install]
WantedBy=default.target
