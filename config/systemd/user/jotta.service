[Unit]
Description=Jottacloud Daemon inside Arch Distrobox
After=podman.service

[Service]
Type=simple
ExecStart=/usr/bin/distrobox-enter -n arch -- jottad
Restart=always

[Install]
WantedBy=default.target
