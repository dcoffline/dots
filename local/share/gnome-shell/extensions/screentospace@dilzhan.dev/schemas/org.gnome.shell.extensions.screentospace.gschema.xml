<?xml version="1.0" encoding="UTF-8"?>
<!--
  ScreenToSpace - GSettings Schema
  
  @author DilZhaan
  @license GPL-2.0-or-later
-->
<schemalist>
  <schema id="org.gnome.shell.extensions.screentospace" path="/org/gnome/shell/extensions/screentospace/">
    <key name="move-window-when-maximized" type="b">
      <default>true</default>
      <summary>Move window when maximized</summary>
      <description>
        Automatically move maximized windows to empty workspaces.
        When enabled, both maximized and fullscreen windows will be moved.
        When disabled, only fullscreen windows will be moved.
      </description>
    </key>

    <key name="trigger-on-maximize" type="b">
      <default>true</default>
      <summary>Move window on maximize</summary>
      <description>
        When enabled, maximizing a window triggers ScreenToSpace workspace isolation.
      </description>
    </key>

    <key name="trigger-on-fullscreen" type="b">
      <default>true</default>
      <summary>Move window on fullscreen</summary>
      <description>
        When enabled, entering fullscreen triggers ScreenToSpace workspace isolation.
      </description>
    </key>

    <key name="override-modifier" type="s">
      <default>'none'</default>
      <summary>Override modifier</summary>
      <description>
        Hold this modifier while maximizing or fullscreening to bypass ScreenToSpace and use GNOME's default behavior.
      </description>
      <choices>
        <choice value='none'/>
        <choice value='alt'/>
        <choice value='super'/>
        <choice value='ctrl'/>
        <choice value='shift'/>
      </choices>
    </key>

    <key name="disable-on-external-monitor" type="b">
      <default>false</default>
      <summary>Disable when external monitor is connected</summary>
      <description>
        When enabled, ScreenToSpace does not move windows while more than one monitor is connected.
      </description>
    </key>

    <key name="insert-workspace-after-current" type="b">
      <default>false</default>
      <summary>Insert workspace after current</summary>
      <description>
        When enabled, ScreenToSpace tries to place isolated windows on an existing empty workspace directly after the current workspace.
        If that safe adjacent placement is not available, ScreenToSpace falls back to the standard free-workspace behavior.
      </description>
    </key>

    <key name="triggers-migrated" type="b">
      <default>false</default>
      <summary>Internal: triggers migrated</summary>
      <description>
        Internal flag used to migrate legacy maximize/fullscreen behavior to the new trigger toggles.
      </description>
    </key>

    <key name="filter-mode" type="s">
      <default>'blacklist'</default>
      <summary>App filtering mode</summary>
      <description>
        Controls whether the extension ignores apps in a blacklist or only manages apps in a whitelist.
        Use 'blacklist' to skip listed apps. Use 'whitelist' to manage only listed apps.
      </description>
      <choices>
        <choice value='blacklist'/>
        <choice value='whitelist'/>
      </choices>
    </key>

    <key name="blacklist-apps" type="as">
      <default>[]</default>
      <summary>Blacklisted applications</summary>
      <description>
        Desktop app IDs to ignore. When filter-mode is set to blacklist, windows from these apps will not be managed.
      </description>
    </key>

    <key name="whitelist-apps" type="as">
      <default>[]</default>
      <summary>Whitelisted applications</summary>
      <description>
        Desktop app IDs to manage. When filter-mode is set to whitelist, only windows from these apps will be managed.
      </description>
    </key>
  </schema>
</schemalist>
